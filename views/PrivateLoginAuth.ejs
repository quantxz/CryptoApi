<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Private Login Auth System </title>

    <style>
        *{
            padding: 0;
            margin: 0;
            font-family: 'poppins', sans-serif;
        }
        
        body {
            min-height: 100vh;
            background: linear-gradient(165deg, #FFFFFF 35%, #00FFFF 100%);
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        
        .title {
            position: absolute;
            left: 50%;
            transform: translate(-50%, 55%);
            font-size: 45px;
        }

        .subtitle{
            position: absolute;
            left: 50%;
            top: 10%;
            transform: translate(-50%);
            font-size: 25px;
            color: #898f97;
        }

        .container {
            position: absolute;
            background-color: #FFF;
            width: 25vw;
            height: 60VH;
            border-radius: 1rem;
            top: 60%;
            left: 20%;
            transform: translate(-50%, -50%);
            align-items: center;
            text-align: center;
            box-shadow: -4px -4px 8px rgba(0, 0, 0, 0.2), 4px 4px 8px rgba(0, 0, 0, 0.2);
        }


        .form {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            height: 15vh;
        }

        .form-group {
            display: flex;
            flex-direction: row;
            align-items: center;
        }

        .form label {
            position: relative;
            font-size: 25px;
            padding-right: 6rem;
            margin-top: 2rem;
            margin-left: 2rem;
        }

        .form input {
            position: absolute;
            font-size: 25px;
            margin-top: 2rem;
            margin-left: 12rem;
            width: 14vw;
            border: none;
            background-color: #d0e1f7;
            border-radius: 2px;
        }

        .form input::placeholder {
            font-size: 16px; /* Defina o tamanho da fonte desejado */
            padding-left: 10px;
        }

        .form > .submitButton {
            position: relative;
            background-color: #00FFFF;
            color: #0e6666;
            border-radius: 3px;
            top: 15rem;
            font-size: 35px;
            margin-left: auto;
            margin-right: auto;
            cursor: pointer;
            width: 15vw;
        }

    </style>
</head>
<body>
    <h1 class="title">Api Key Autentication</h1>
    <h2 class="subtitle">Para acessar a Api conclua os dados Abaixo</h2>
    <div class="container">
        <form action="/apiKeySecond" method="post" class="form">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="text" id="email" placeholder="Email">
            </div>
            <div class="form-group">
                <label for="senha">Password</label>
                <input type="password" id="senha" placeholder="Password">
            </div>

            <input type="submit" value="Enviar" class="submitButton">
        </form>
    </div>
</body>
</html>